为啥使用优先队列？（主要动态）

在n个元素中选出前m个元素
使用优先队列时间复杂度NlogM
优先队列主要操作
入队
出队（取出优先级最高的元素）

使用堆实现优先队列：对于总共N个请求：使用普通数组或者顺序数组，
最差情况：O(n^2)．使用堆：O(nlogn).

二叉堆是一颗完全二叉树：堆中某个节点的值总是不大于其父节点的值；
堆总是一颗完全二叉树．（最大堆）

用数组存储二叉堆

shift up：对一个堆添加一个元素，看父结点和新加入的结点比大小，一直循环到合适的位置
shift down：对一个堆中取出一个元素，只能取出根节点的元素，把最后一个元素放到根
